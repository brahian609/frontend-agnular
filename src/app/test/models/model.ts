export interface Model {
  id: number,
  authNum: number,
  createdBy: string,
  requestNum: number,
  customer: string,
  trip: number,
  tail: string,
  operator: string,
  billTo: string,
  creditStatus: string,
  dateFrom: Date,
  dateTo: Date
}
